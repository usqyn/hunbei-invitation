# ⚠️ 本文件属于本地环境配置，已被 .gitignore 的 .env.* 规则忽略，请勿提交真实密钥。
#
# ─── dev 与生产的请求链路不同（重要，排查问题时必看）───
# 本文件未设置 VITE_USE_CLOUD，因此 USE_CLOUD_FUNCTIONS=false，
# 请求走 uni.request → API_BASE（本地/局域网后端），便于本地联调 Express 后端。
# 生产（.env.production）设置 VITE_USE_CLOUD=1，请求走 wx.cloud.callFunction。
# 两条链路代码不同，本地正常不代表体验版正常。
# 若需在开发者工具中调试云函数链路，临时取消下一行注释即可：
# VITE_USE_CLOUD=1

VITE_API_BASE=

# 微信小程序 dev 模式 → 真机调试/预览时用局域网 IP（手机必须连同一 WiFi）
VITE_WECHAT_DEV_API=http://192.168.31.175:3001

# 微信小程序 appid（非敏感信息；无 VITE_ 前缀，不会被打包进前端）
WECHAT_APPID=wxac46cbfe019159a6

# ⚠️ AppSecret 是服务端密钥，绝不应放在前端 env 文件中。
# 此处已移除。请立即到微信公众平台重置 AppSecret，
# 并只在服务端 / 云函数（环境变量或云开发配置）中保存。
